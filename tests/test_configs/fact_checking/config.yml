models:
  - type: main
    engine: openai
    model: gpt-3.5-instruct-turbo

rails:
  config:
    fact_checking:
      parameters:
        endpoint: "http://localhost:5000/alignscore_base"
      fallback_to_self_check: True

  output:
    flows:
      - check facts
